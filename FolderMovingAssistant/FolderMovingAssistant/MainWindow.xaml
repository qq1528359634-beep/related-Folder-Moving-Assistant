<Window x:Class="FolderMovingAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FolderMovingAssistant"
        mc:Ignorable="d"
        Title="Folder" Height="600" Width="800" Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <GroupBox Header="PathSet" Grid.Row="0" Padding="10">
            <StackPanel>
                <DockPanel Margin="0,5">
                    <TextBlock Text="母目录:" Width="50" />
                    <Button Content="Select MonFloder" DockPanel.Dock="Right"  Click="SelectMonPath_Click" Width="110" Height="20"/>
                    <TextBox x:Name="monPathBox"  Margin="5,0" DockPanel.Dock="Left" AcceptsReturn="True"  IsReadOnly="True"   Text="Please Select Path"  />
                </DockPanel>
                <DockPanel Margin="0,5">
                    <TextBlock Text="子目录:" Width="50"/>
                    <Button Content="Select SonFloder" DockPanel.Dock="Right" Click="SelectSonPath_Click" Width="110" Height="20"/>
                    <TextBox x:Name="sonPathBox" Margin="5,0" DockPanel.Dock="Left" AcceptsReturn="True"  IsReadOnly="True"   Text="Please Select Path"/>
                </DockPanel>
            </StackPanel>
        </GroupBox>
        <TextBlock Grid.Row="1" Text="匹配模式：模糊包含（只要目标文件夹名包含源名即移动" Foreground="Gray" Margin="0,10,0,10" FontStyle="Italic"/>
        <ListView x:Name="sonFolderList" Grid.Row="2" Margin="10">
            <ListView.View>
                <GridView>
                    <!-- 第一列：序号 -->
                    <GridViewColumn Header="Count" Width="60" DisplayMemberBinding="{Binding Id}"/>

                    <!-- 第二列：内容 -->
                    <GridViewColumn Header="Folder Name" Width="700" DisplayMemberBinding="{Binding Text}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button x:Name="Mo" Content="Moving floders" Grid.Row="3" Height="40" Width="150" Background="#007ACC" Foreground="White" HorizontalAlignment="Right" Click="Move_Click"/>

    </Grid>
</Window>
