<Window x:Class="FolderMovingAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FolderMovingAssistant"
        mc:Ignorable="d"
        Title="FoldeMovingAssistanc" Height="600" Width="800" Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <GroupBox Header="PathSet" Grid.Row="0" Padding="10">
            <StackPanel>
                <DockPanel Margin="0,5">
                    <TextBlock Text="Targer Path:" Width="80" />
                    <Button Content="Select..." DockPanel.Dock="Right"  Click="SelectMonPath_Click" Width="110" Height="20"/>
                    <TextBox x:Name="targerPathBox"  Margin="5,0" DockPanel.Dock="Left" AcceptsReturn="True"  IsReadOnly="True"   Text="Please Select Path"  />
                </DockPanel>
                <DockPanel Margin="0,5">
                    <TextBlock Text="Source Path:" Width="80"/>
                    <Button Content="Select..." DockPanel.Dock="Right" Click="SelectSonPath_Click" Width="110" Height="20"/>
                    <TextBox x:Name="sourcePathBox" Margin="5,0" DockPanel.Dock="Left" AcceptsReturn="True"  IsReadOnly="True"   Text="Please Select Path"/>
                </DockPanel>
            </StackPanel>
        </GroupBox>
        <TextBlock Grid.Row="1" Text="Matching pattern:sourceFolder contains targetFolder name (sourceFolder moving to targerFolder) &#10; and the targetfolder and source folder are matched one-to-one. " Foreground="Gray" Margin="0,10,0,10" FontStyle="Italic"/>
        <ListView x:Name="sourceFolderList" Grid.Row="2" Margin="10">
            <ListView.View>
                <GridView>
                    <!-- 第一列：序号 -->
                    <GridViewColumn Header="Count" Width="60" DisplayMemberBinding="{Binding Id}"/>

                    <!-- 第二列：内容 -->
                    <GridViewColumn Header="Folder Name" Width="700" DisplayMemberBinding="{Binding Text}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button x:Name="Mo" Content="Moving floders" Grid.Row="3" Height="40" Width="150" Background="#007ACC" Foreground="White" HorizontalAlignment="Right" Click="Move_Click"/>

    </Grid>
</Window>
